{% extends '::base.html.twig' %}

{% block body -%}
    <h1>QuestionTicket list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Question</th>
                <th>Creationdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('questionticket_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.question }}</td>
                <td>{% if entity.creationDate %}{{ entity.creationDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('questionticket_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('questionticket_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('questionticket_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
