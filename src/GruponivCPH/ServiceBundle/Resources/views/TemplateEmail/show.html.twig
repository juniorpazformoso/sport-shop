{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TemplateEmail</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Activate</th>
                <td>{{ entity.activate }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ entity.subject }}</td>
            </tr>
            <tr>
                <th>Typeemail</th>
                <td>{{ entity.typeemail }}</td>
            </tr>
            <tr>
                <th>Recipient</th>
                <td>{{ entity.recipient }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('templateemail') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('templateemail_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
